# docker compose 變數（全部可選）。複製成 .env，不設定就用內建預設值。
# ⚠️ 這個 .env 會被 docker compose 讀取做變數代換，請勿在此設 localhost
#    （容器內 localhost 不是主機）。

# WebUI 對外 port（預設 5273）→ http://localhost:<port>
# WEBUI_PORT=5273

# 後端連到的 ollama / A1111 位址。
# 預設透過 host.docker.internal 連到「主機上」ollama/A1111 容器發佈的 port。
# 只有當這兩個服務不在本機、或想指定別的位址時才需覆寫：
# OLLAMA_URL=http://host.docker.internal:11434
# A1111_URL=http://host.docker.internal:7860

# 生成逾時秒數（預設 600）
# HTTP_TIMEOUT=600

# ── Claude CLI 引擎（可選）────────────────────────────────────────
# 要用「已登入的 claude（Claude Code）」當 AI 引擎（與 ollama 二選一）時，
# 把主機的認證目錄掛進容器。用「絕對路徑」指到你的 ~/.claude
# （sudo 執行 compose 時 $HOME 會變 /root，務必寫絕對路徑）：
# CLAUDE_CREDS_DIR=/home/你的帳號/.claude
#
# 可選：下拉可選的 Claude 模型（預設 sonnet,opus,fable,haiku）。
# 別名由 claude CLI 解析成該系列最新版（sonnet→Sonnet 5、opus→Opus 5、
# fable→Fable 5.1、haiku→Haiku 4.5）；也可填完整 ID 固定版本，例如
# claude-fable-5-1 / claude-opus-5 / claude-opus-4-8 / claude-opus-4-7 /
# claude-sonnet-5 / claude-sonnet-4-6 / claude-haiku-4-5；
# 200K 模型可加 [1m] 後綴開 1M 視窗（sonnet[1m]、claude-opus-4-6[1m]）。
# CLAUDE_MODELS=sonnet,opus,fable,haiku
# CLAUDE_MODELS=fable,opus,claude-opus-4-8,sonnet,haiku[1m]
# 可選：所有 Claude 模型一律用此 context 視窗（預設 0＝依模型：1M 或 200K）
# CLAUDE_CONTEXT_LENGTH=0
# 可選：claude 執行檔路徑（預設用映像內安裝的 claude）
# CLAUDE_BIN=claude

# ── OpenAI Codex CLI 引擎（可選）──────────────────────────────────
# 要用「已登入的 codex（OpenAI Codex CLI）」當 AI 引擎時，把主機的 ~/.codex
# 掛進容器（裡面同時有 static 執行檔、auth.json 與模型快取）。用「絕對路徑」：
# CODEX_CREDS_DIR=/home/你的帳號/.codex
#
# 可選：下拉模型（預設自動讀 ~/.codex/models_cache.json 裡帳號可用的模型；要固定才設）。
# 2026-09 目錄：gpt-6-astra、gpt-5.6-sol、gpt-5.6-terra、gpt-5.6-luna、gpt-5.5、gpt-5.4-mini
# CODEX_MODELS=gpt-6-astra,gpt-5.6-sol,gpt-5.6-terra,gpt-5.6-luna,gpt-5.5,gpt-5.4-mini

# ── 提示詞歷史（sd-webui-prompt-history 擴充）──────────────────────
# 想在 WebUI 直接瀏覽／重用 A1111「prompt-history」擴充記錄的歷史時，把該擴充的
# data 目錄（含 data.json 與 <id>.jpg）唯讀掛進容器。用「絕對路徑」指到它：
# PROMPT_HISTORY_DIR=/home/你的帳號/.../sd-webui-forge-neo/extensions/sd-webui-prompt-history/data
# 未設則頂欄不顯示「歷史」按鈕（不影響其他功能）。
# 此值也可在 ⚙️ 設定面板選擇（會覆寫這裡並持久化）；docker 模式下仍只能選「已掛載進容器」的路徑。

# ── 目錄瀏覽 / 圖片儲存 白名單 ─────────────────────────────────────
# /api/browse 與圖片儲存位置只能落在這些根目錄內（冒號分隔）。
# 預設：家目錄與 DATA_DIR。需要放更多位置時才設：
# BROWSE_ROOTS=/home/你的帳號:/data:/mnt/nas/images

# ── 本機開發（不走 docker、直接跑 uvicorn）時，後端預設就連 localhost，
#    通常不需任何設定。後端也會直接讀這個 .env（已存在的環境變數優先），
#    但注意上方 docker 用的 host.docker.internal 位址在本機開發下不適用。
